spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nelo
    username: nelo
    password: nelo
  sql:
    init:
      mode: never
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

